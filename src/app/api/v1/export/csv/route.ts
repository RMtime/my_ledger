import { requireUnlockedUser } from "@/modules/vault/http";
import { errorResponse } from "@/modules/shared/errors";
import { listTransactions } from "@/modules/ledger/service";

const safe = (value: unknown) => { let text = typeof value === "bigint" ? value.toString() : String(value ?? ""); if (/^[=+\-@\t\r]/.test(text)) text = `'${text}`; return `"${text.replaceAll('"','""')}"`; };
export async function GET(request: Request) { try { const actor = await requireUnlockedUser(request); const columns = ["id","kind","amount_minor","currency","occurred_at","occurred_timezone","time_precision","category_id","payment_method","payment_method_id","account_id","channel_id","merchant","note","source","version","deleted_at"]; const rows: Array<Record<string, unknown>> = []; let cursor: string | null = null; do { const page = listTransactions(actor, { limit: 100, cursor: cursor ?? undefined }); rows.push(...page.items as Array<Record<string, unknown>>); cursor = page.next_cursor; } while (cursor); const csv = [`\ufeff${columns.join(",")}`, ...rows.map((row) => columns.map((c) => safe(row[c])).join(","))].join("\r\n"); return new Response(csv, { headers: { "content-type": "text/csv; charset=utf-8", "content-disposition": `attachment; filename="ledger-${new Date().toISOString().slice(0,10)}.csv"`, "cache-control": "no-store" } }); } catch (error) { return errorResponse(error); } }
