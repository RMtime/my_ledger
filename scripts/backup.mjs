import Database from "better-sqlite3";
import { mkdirSync } from "node:fs";
import { dirname, resolve } from "node:path";
const source=resolve(process.env.DATABASE_PATH??"./data/ledger.db");const target=resolve(process.argv[2]??`./backups/ledger-${new Date().toISOString().replaceAll(":","-")}.db`);mkdirSync(dirname(target),{recursive:true});const db=new Database(source,{readonly:true});await db.backup(target);db.close();const restored=new Database(target,{readonly:true});const integrity=restored.pragma("integrity_check",{simple:true});const count=restored.prepare("SELECT COUNT(*) count FROM transactions").get().count;const totals=restored.prepare("SELECT currency,kind,SUM(amount_minor) amount_minor FROM transactions WHERE deleted_at IS NULL GROUP BY currency,kind ORDER BY currency,kind").all();restored.close();if(integrity!=="ok")throw new Error(`Backup integrity failed: ${integrity}`);console.log(JSON.stringify({target,integrity,transaction_count:String(count),totals:totals.map(row=>({...row,amount_minor:String(row.amount_minor)}))},null,2));
